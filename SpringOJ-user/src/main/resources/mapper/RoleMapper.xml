<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.david.mapper.RoleMapper">
    <select id="findRolesByUserId" resultType="com.david.entity.role.Role">
        SELECT r.*
        FROM role r
                 INNER JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
    <select id="selectRolePage" resultType="com.david.entity.role.Role">
        SELECT *
        FROM role
        <where>
            <if test="keyword != null and keyword != ''">
                (role_name LIKE CONCAT('%', #{keyword}, '%') OR remark LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id
    </select>
</mapper>
